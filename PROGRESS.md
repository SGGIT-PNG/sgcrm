# SG솔루션 CRM v3 — 진행 상황

> **PC와 노트북 두 대에서 번갈아 작업합니다.**
> 작업 시작 전 `git pull` / 작업 종료 시 이 파일 갱신 → "올려줘" 지시 시 push.
>
> **최종 갱신: 2026-07-31 (노트북)**

---

## 1. 현재 상태

| 항목 | 값 |
|---|---|
| 정본 파일 | `index.html` — **8,442줄** |
| 최신 커밋 | `1908003` — 캘린더 뷰 구글 일정 아이콘 제거 (2026-07-31) |
| origin 동기화 | `main` ↔ `origin/main` 일치 |
| 워킹트리 | ⚠️ **미커밋 변경 있음** — `index.html`(ToDo 전송) / `CLAUDE.md` / `PROGRESS.md` |
| 검증 | `node verify.mjs` **전 항목 통과** (함수 288개 / export 146개) |
| node / git | v24.16.0 / 2.55.0.windows.3 |
| Firebase | `sg-crm-f9adc` (Seoul) |
| 원격 | https://github.com/SGGIT-PNG/sgcrm |

**앱 코드는 현재 전부 완료·푸시된 상태입니다.** 남은 작업은 §4를 보세요.

---

## 2. 완료된 작업

### Task 2 (A~E) — 통합심사 기반 구축
| 항목 | 내용 | 커밋 |
|---|---|---|
| A/B | 통합심사 입력 UI (ISO 대분류에 1차·2차 심사일 + 심사일수) | — |
| C | 인증완료 화면에 통합심사 규격 자동 반영 | `3325b03` |
| D | ISO 갱신주기 **4년 → 3년** 변경 + 기존 데이터 자동 재계산 | `b9f4c30` |
| E | 캘린더(Apps Script) 연동 — 심사 일정 push | — |

### Phase F — 사이클 관리
| 항목 | 내용 | 커밋 |
|---|---|---|
| F.1 | 인증 목록 통합심사 **사이클 그룹핑** 표시 | `f4a898d` |
| F.2 | 사이클 식별자 **`cycleGroupId`** 추가 | `3c20324` |
| F.3+F.4 | 단계 완료 시 규격 선택(빼기·부분 갱신) **모달** | `46d7889` |

→ `openCyclePhaseDoneModal(cycleGroupId, phaseIndex)` (L5585) / `submitCyclePhaseDone()` (L5629)

### 데이터 정합성
| 내용 | 커밋 |
|---|---|
| 간트/업무 삭제 시 연관 인증·연간신고·캘린더 **cascade 삭제** | `13ffb5d` |
| **고아 인증서 자동 정리** | `6305554` |
| 고아 정리 시 `consaudit`/`cons` 캘린더 이벤트 동반 삭제 | `996086c` |
| ISO **심사일 겹침 하드 블록** (범위+일수 반영) | `71fa4e2` |

---

### ✅ 간트 캘린더 뷰 — **완료 (2026-07-30 ~ 07-31)**

간트 페이지에 구글 캘린더 스타일 월 그리드를 추가. 뷰 토글 `[간트][캘린더]`.
**요구사항 전부 구현·검증 완료.**

| 요구사항 | 상태 |
|---|---|
| 뷰 토글 `[간트][캘린더]` | ✅ `setGanttView()` (L3661), 모드를 `app_state`에 영속화 |
| 이벤트: 대분류(지원사업) | ✅ 기간 막대 |
| 이벤트: ISO 1차·2차 심사 | ✅ **별도 막대**로 분리 표시 |
| 이벤트: 인증만료 | ✅ |
| 이벤트: ToDo | ✅ **`dueDate` 필드 신설** (모달에 마감일 입력, 수정 모드 지원) |
| 이벤트: 연간신고 | ✅ |
| 이벤트 클릭 → 기존 편집 모달 | ✅ |
| 상태 필터를 간트와 공유 | ✅ `consPassesGanttFilter(c,ccs)` (L3052)로 상태·지역·업종 공유 |

추가 구현:
- **레인 배정** — 다일 이벤트가 여러 날·여러 주에 걸쳐 항상 같은 줄에 정렬.
  셀당 3줄 초과 시 `+N개` → 팝오버 (`assignEventLanes` L3402, `openGcalDay` L3621)
- **`[월][주]` 토글** — 기본 **월 뷰**(한 줄=한 주, 일반 달력).
  주 뷰는 24시간 시간표(1시간 42px, 오전 7시 자동 스크롤, 겹침 가로 분할, 현재 시각 빨간 선)
- ToDo 기존 데이터는 `dueDate` 없음 → 캘린더 미표시 (하위 호환, 마이그레이션 불필요)

> 📌 초기에 "주단위 캘린더" 요청을 **구글의 Week 시간표 뷰로 잘못 구현**했다가,
> 실제 의도가 *한 줄이 한 주인 일반 달력*이어서 월 뷰 기본으로 정정.
> 저장 키를 `gcalMode` → `calFormat`으로 바꿔 기본이 week였던 시절의 잔여값이 무시되게 함.

브라우저 실검증 (실 Firestore 데이터: 지원사업 14 / 인증 53 / ToDo 3) —
2026-06 기준 48세그먼트·`+N개` 4건 정상, 팝오버 4건, 7열 균등(99px), **콘솔 오류 0**.

---

### ✅ 구글 캘린더 연동 — **완료 (2026-07-30 ~ 07-31)**

**① 앱 → 구글 (쓰기)** — 이전부터 정상 동작 중
`sendToGcal` (L8457) / `gcalSyncCert` `gcalSyncAnnual` `gcalSyncCons` / `syncGcalAll` (L8553)

**② 구글 → 앱 (읽기)** — 폰·PC로 넣은 개인 일정을 앱 캘린더에 함께 표시
- `SG_CRM_Calendar_v8_Code.gs` 신규 (v7 → v8): `doGet?action=list&from=&to=` 추가
- CRM이 만든 일정은 ScriptProperties 태그 + 제목 접두어로 **제외** (중복 방지)
- `doPost`가 FormData(multipart)도 파싱하도록 보강
- 앱: `loadGcalExternal()` (L3268) + `gcalExternal` 캐시, `collectGanttEvents`에 병합
- 헤더에 `구글 일정` 체크박스 + 상태 표시 — **조회 실패를 조용히 넘기지 않음**
- 앱 내장 안내 코드(`getAppsScriptCode` L8588)가 v6으로 낡아 있어 v8로 교체

**③ 설정을 localStorage → Firebase로 이전**
기존엔 웹훅 URL이 localStorage에 있어 **PC 2대 + 폰에서 각각 입력**해야 했음.
→ `app_state/config.gcalSettings`로 옮겨 **한 번만 입력하면 모든 기기 적용**.
- `appStatePayload()` (L2849) 추출 — app_state는 `setDoc`(전체 덮어쓰기)이라 저장 항목을
  한 곳에서 관리해야 함 (누락 시 조용히 삭제됨)
- `loadAll`에 **1회 이전 로직**(idempotent) — Firestore에 없고 localStorage에 있으면 옮김
- **`연동 해제` 버튼 신설** — 기존엔 한번 설정하면 끌 방법이 없었음.
  구글의 기존 일정은 지우지 않고 앱↔구글 연결만 끊음

**④ 실동작 확인 (2026-07-31)** ✅
- Apps Script 배포 완료 → `doGet` 응답 `SG CRM Calendar OK (list 지원)`
- 웹훅 URL을 앱에 저장 (Firebase `app_state.gcalSettings`) → 모든 기기 적용
- `action=list` **47건 반환 / CRM 일정 누출 0건**
- 앱 캘린더(2026-08)에 구글 개인 일정 **23건** + CRM **2건** 정상 표시

---

### ✅ ToDo → 구글 캘린더 전송 — **앱 코드 완료 (2026-07-31, 노트북)**

체크박스·설정 저장·타입맵·Apps Script 분기는 이미 있었는데 **앱에서 보내는 함수만 없어**
설정에서 `ToDo`를 켜도 아무 일도 일어나지 않던 상태였음. 그 빠진 고리를 채움.

| 추가/수정 | 위치 |
|---|---|
| **`gcalSyncTodo(todo,action)`** 신규 | `index.html` (`addDaysISO` 바로 위) |
| `saveTodo()` — 신규 저장 시 `addDoc` 반환 ref에서 id를 받아 전송 | `saveTodo` |
| `cycleTodo()` — 상태 순환 후 전송 | `cycleTodo` |
| `deleteTodoItem()` — 삭제 시 캘린더 이벤트도 제거 | `deleteTodoItem` |
| `syncGcalAll()` / `syncGcalType()` — **수동 전체 동기화에 `todo` 분기 추가** | `syncGcalAll` L~8563 |

**동작 규칙** (기존 연간신고 방식과 맞춤):
- 마감일 **없음** → 캘린더에서 제거 (마감일을 나중에 지운 경우도 자동 정리)
- 상태 **완료(done)** → 캘린더에서 제거. 다시 `대기`/`진행`으로 돌리면 재등록됨
- 전체 동기화 대상은 `dueDate` 있고 `done`이 아닌 ToDo만

> 📌 **완료 시 구글 일정을 지우는 것은 판단해서 넣은 규칙**입니다.
> 연간신고가 `status!=='done'`만 동기화하는 것과 맞췄고, 끝난 할일이 캘린더에 쌓이지 않게 하려는 의도.
> 앱 안의 캘린더 뷰는 완료 ToDo도 상태 색으로 계속 보여줍니다 (구글에서만 사라짐).
> 완료된 것도 구글에 남기길 원하시면 `gcalSyncTodo`의 `todo.status==='done'` 조건만 빼면 됩니다.

검증: `node verify.mjs` 전 항목 통과 (함수 287 → **288**개, export 146개 유지 —
`gcalSyncTodo`는 인라인 핸들러가 아니라 내부 호출이므로 window export 불필요).

⚠️ **실동작은 미확인** — 배포된 Apps Script에 `todo` 분기가 있어야 합니다. §4-⓪ 참조.

---

### ✅ 주 뷰 열 정렬 — **확인 완료 + 버그 2건 수정 (2026-07-31, 노트북)**

미검증으로 남아 있던 스크롤바 보정([`renderGcalWeek`](index.html) 끝부분)을 브라우저에서
실측한 결과, **눈에 잘 안 띄는 오차 2건**을 찾아 고쳤습니다.

**버그 ①: 정수 반올림 때문에 열이 조금씩 밀림**
`sc.offsetWidth-sc.clientWidth`는 **정수로 반올림**됩니다. 이 PC에서 실제 스크롤바는
**4.667px**인데 **4px**로 계산돼, 열마다 0.1px씩 누적되어 마지막 토요일 열이 **0.6px** 밀렸습니다.
→ 스크롤 컨테이너 폭 − 내부 그리드 폭을 `getBoundingClientRect()`로 재서 **소수점까지 반영**.

**버그 ②: 종일 영역 이중 보정 — 최대 4px 어긋남**
`.gwk-allday`는 `max-height:104px; overflow-y:auto`라 종일 일정이 많으면(레인 5줄 초과)
**자체 스크롤바**가 생깁니다. 그 위에 보정 padding까지 더해져 **두 번 좁아지면서
마지막 열이 4px(= 스크롤바 한 개 폭) 어긋났습니다.**
→ 종일 영역이 스크롤 중이면 그 스크롤바가 이미 같은 폭을 차지하므로 **보정을 넣지 않음**.

실측 결과 (실 Firestore 데이터, 2026-07-26~08-01 주):

| 상황 | 수정 전 | 수정 후 |
|---|---|---|
| 종일 영역 스크롤 없음 | 헤더 0 → 0.6px 누적 | **7열 전부 0** |
| 종일 영역 스크롤 있음 | 0 → **−4.0px** 누적 | **7열 전부 0** |

주 뷰 골격(7열·24시간·오전 7시 자동 스크롤 `scrollTop=294`), 현재 시각 빨간 선,
시간 일정 배치 정상. **콘솔 오류 0.**

> 점검 중 뷰 모드를 바꾼 뒤 **원래 상태(캘린더 뷰 / 월 모드 / 구글 일정 ON)로 복구 완료.**
> `calY`/`calM`은 건드리지 않았고(`gcalToday`는 주 모드에서 `calWeekAnchor`만 변경),
> 업무 데이터 영향 없음.

---

**⑤ 검증 자동화** — `verify.mjs` 신규 (`check.mjs` 대체)
인라인 핸들러가 부르는 함수의 window export 여부를 대조하는 검사를 추가했고,
이 검사로 **기존 버그 1건 발견·수정**: 인증 알림 탭 검색창(`oninput="renderCertAlert()"`)이
export 누락으로 **타이핑해도 반응하지 않던 상태**였음.

---

## 3. 알려진 배경 지식 (중요)

### Apps Script 프로젝트가 두 개다 ⚠️

| 프로젝트 | 상태 |
|---|---|
| **`SG CRM 동기화`** | **현재 살아있는 것.** 이것만 건드릴 것 |
| `SG솔루션 CRM` | 옛것. `doPost` 없음 → 더 이상 새 일정을 만들지 못함. **손대지 말 것** |

각 프로젝트는 자기가 만든 일정 ID만 ScriptProperties에 기억 → **서로의 일정을 못 지움.**
이것이 `시작`/`마감` 중복 일정의 원인 (§4-② 참조).

### 배포본 ≠ 저장소 파일 ⚠️

배포된 Apps Script는 저장소의 v6/v7과 **다른 버전**이었습니다 (응답 문구부터 달랐고,
저장소 v6은 지원사업을 옛 `p.date`로 읽는데 배포본은 정상 처리 중이었음).
→ **배포본에 우리가 모르는 커스터마이즈가 있을 수 있음.**
그래서 전체 교체(v8) 대신 `SG_CRM_Calendar_ADD_list.gs`(최소 덧붙이기 패치)를 썼습니다.
앞으로도 Apps Script는 **통째로 교체하지 말고 최소 패치**로 접근할 것.

> 배포 URL은 보안상 저장소에 기록하지 않습니다. (Firebase `app_state.gcalSettings`에 있음)

---

## 4. 다음 할 일

### ⓪ [사장님] 배포본에 `todo` 분기가 있는지 확인 — ToDo 전송 실동작 조건

앱 쪽 `gcalSyncTodo` 배선은 **2026-07-31 완료**(§2 참조). 다만 **배포된 Apps Script의
`upsertEvent`에 `type==="todo"` 분기가 있는지 확인되지 않았습니다.**

- 저장소 `SG_CRM_Calendar_v8_Code.gs:130` 에는 있음
- 그러나 배포본은 저장소 파일과 다른 버전 (§3 참조). 읽기 패치(`ADD_list.gs`)는
  `doGet`만 바꿨으므로 `upsertEvent`는 손대지 않았음
- 분기가 없으면 `date`가 undefined → `if(!date...)return` 으로 **조용히 무시됨**
  (오류는 안 나지만 구글 캘린더에 ToDo가 안 생김)

**확인 방법**: 앱에서 마감일 있는 ToDo를 하나 저장 → 구글 캘린더에 `[ToDo] …` 가 뜨는지 확인.
안 뜨면 `upsertEvent`에 아래 5줄만 덧붙이고 **새 버전으로 재배포** (기존 코드 건드리지 말 것):

```js
  else if(type==="todo"){
    title="[ToDo] "+p.title;
    date=new Date(p.dueDate);
    desc=p.title;
    color=CalendarApp.EventColor.GREEN;
    alarms=[1*24*60];
  }
```

### ① [Claude, 사장님 승인 필요] ISO 심사 기간 수정 — Apps Script

배포된 코드의 `cert` 분기가 앱이 보낸 `p.endDate`를 무시하고 있습니다:

```js
if (type === 'cert') {
  startDate = new Date(p.expDate);
  endDate   = new Date(p.expDate);   // ← p.endDate를 안 씀
```

→ **3일짜리 ISO 심사도 구글 캘린더에 하루로만 표시됨.** 3줄 수정이면 해결.
고치면 다음 동기화 때 기존 일정이 자동으로 기간으로 늘어납니다.

### ② [Claude, 사장님 승인 **필수**] `시작`/`마감` 중복 일정 정리

- 옛 프로젝트(`SG솔루션 CRM`) → `p.isStart` 방식으로 `시작`/`마감` **2건** 생성
- 현재 프로젝트(`SG CRM 동기화`) → `startDate~endDate` 기간 **1건** 생성
- 확인 사례: `지식재산긴급지원 3차(서울)` 6/30~7/21 — 기간 1건 + 시작/마감 2건 공존

옛 프로젝트는 `doPost`가 없어 **더 이상 새로 만들지 못함** → 일회성 정리로 끝납니다.
**구글 캘린더 일정 삭제 작업이므로 반드시 목록을 먼저 뽑아 사장님 확인 후 진행.**

### ③ 기타 (급하지 않음)

- ~~주 뷰 헤더/시간그리드 열 정렬 재확인~~ → **2026-07-31 확인 + 버그 2건 수정** (§2)
- ~~ToDo → 구글 캘린더 전송(`gcalSyncTodo`) 배선~~ → **2026-07-31 완료** (§2)

> 현재 남은 것은 ⓪①② 뿐이며 **셋 다 사장님 조치가 선행**되어야 합니다.

---

## 5. 세션 로그

> 작업 종료 시 아래에 한 줄씩 추가합니다. **형식: `### YYYY-MM-DD (기기) — 요약`**

### 2026-07-31 (노트북) — 주 뷰 열 정렬 검증 + 버그 2건 수정
- 브라우저 실측으로 확인: 정수 반올림(0.6px) / 종일 영역 이중 보정(4px) 두 건 발견·수정
- 수정 후 두 상황 모두 **7열 오차 0**, 콘솔 오류 0. 앱 상태는 점검 전으로 복구

### 2026-07-31 (노트북) — ToDo → 구글 캘린더 전송 구현
- `gcalSyncTodo()` 신규 + `saveTodo`/`cycleTodo`/`deleteTodoItem` 배선
- 수동 전체 동기화(`syncGcalAll`/`syncGcalType`)에 `todo` 분기 추가
- 마감일 없음·완료 상태 → 캘린더에서 자동 제거
- `node verify.mjs` 통과 (함수 288개). **실동작은 배포본 `todo` 분기 확인 필요 → §4-⓪**

### 2026-07-31 (노트북) — 문서 정비, 작업 규칙 변경
- `git pull`로 PC 작업분 수신 (`edf231f` → `1908003`, 6파일 +1549/-117)
- 상태 점검: 워킹트리 clean / git identity 일치 / node v24.16.0 / `verify.mjs` 전 항목 통과
- `index.html` 구조 재파악 → **8,442줄** (직전 기록 8,013줄에서 +429). 코드 구조 지도 줄번호 갱신
- 🔴 **작업 규칙 변경**: 기존 "자동 커밋·푸시(확인 불필요)" → **"push는 사장님이 '올려줘'라고 할 때만"**
- `CLAUDE.md` 전면 개편 (프로젝트 개요 / 작업 규칙 / **멀티 PC 규칙** / index.html 구조 / app_state / 데이터 안전 / 코드 스타일)
- `PROGRESS.md` 재구성 — 캘린더 뷰를 "진행 중" → **완료**로 정정, 세션 로그 형식 도입

### 2026-07-31 (PC) — 구글 캘린더 읽기 연동 실동작 확인
- Apps Script 읽기 패치 배포 완료, `action=list` 47건 / CRM 누출 0건 검증
- 캘린더 뷰에서 구글 일정 아이콘 제거해 제목 표시 공간 확보 (`1908003`)

### 2026-07-30 (PC) — 캘린더 뷰 + 구글 캘린더 연동 구현
- 간트 페이지 캘린더 뷰(월/주), 구글 캘린더 읽기 연동, 설정 Firebase 이전, `verify.mjs` 신규
